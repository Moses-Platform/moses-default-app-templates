{
  "openapi": "3.0.3",
  "info": {
    "title": "My Fullstack App API",
    "description": "TODO: describe your API. Every route registered in cmd/server/demo_routes.go must appear under paths (relative to servers[0].url); the cmd/server tests enforce spec-to-mux consistency.",
    "version": "1.0.0"
  },
  "servers": [
    {
      "url": "/api/v1",
      "description": "Canonical API base. MUST stay base-path-free: the platform's workspace-tool proxy prepends MOSES_BASE_PATH itself (CHAT-8qiu0). Paths below are relative to this base — the platform's openapi_parser folds servers[0].url + path into the registered endpoint path. Never list /health here (it would register a phantom workspace tool)."
    }
  ],
  "paths": {},
  "components": {
    "schemas": {}
  }
}
